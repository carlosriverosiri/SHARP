---
/**
 * OpSmarta - Smärtstillande efter operation
 */
interface Props {
  visaAlvedon?: boolean;
  visaIpren?: boolean;
  visaCitodon?: boolean;
  extraInfo?: string;
}

const { 
  visaAlvedon = true,
  visaIpren = true,
  visaCitodon = true,
  extraInfo
} = Astro.props;
---

<section class="mb-8">
  <div class="bg-white rounded-xl shadow-md border border-gray-100 p-4">
    <!-- Rubrik med ikon -->
    <div class="flex items-center gap-3 mb-3 pb-3 border-b border-gray-100">
      <img src="/images/operation/medicine.svg" alt="" class="w-10 h-10" />
      <h2 class="text-lg font-bold text-[#024264]">Smärtstillande</h2>
    </div>

    <p class="text-slate-600 mb-3">
      Ta smärtstillande <strong>regelbundet de första dagarna</strong> – vänta inte tills det gör ont.
    </p>

    <div class="grid md:grid-cols-3 gap-3 mb-3">
      {visaAlvedon && (
        <div class="bg-blue-50 rounded-lg p-3 border border-blue-100">
          <h3 class="font-semibold text-blue-800">Alvedon®</h3>
          <p class="text-sm text-blue-700">1g × 4/dygn (max 4g)</p>
        </div>
      )}
      
      {visaIpren && (
        <div class="bg-green-50 rounded-lg p-3 border border-green-100">
          <h3 class="font-semibold text-green-800">Ipren®</h3>
          <p class="text-sm text-green-700">400mg × 3/dygn. Med mat.</p>
        </div>
      )}
      
      {visaCitodon && (
        <div class="bg-amber-50 rounded-lg p-3 border border-amber-100">
          <h3 class="font-semibold text-amber-800">Citodon® <span class="font-normal">(recept)</span></h3>
          <p class="text-sm text-amber-700">Max 8 tabl/dygn. Ej bil!</p>
        </div>
      )}
    </div>

    <div class="bg-rose-50 border-l-4 border-rose-400 px-3 py-2 text-sm text-rose-700">
      <strong>⚠️</strong> Kombinera ej Alvedon med andra paracetamolläkemedel. Citodon = ingen bilkörning.
    </div>
  </div>

  {extraInfo && (
    <div class="mt-4 bg-blue-50 border border-blue-200 rounded-xl p-4 text-sm text-blue-800">
      {extraInfo}
    </div>
  )}
</section>

