---
/**
 * OpSmarta - Smärtstillande efter operation
 */
interface Props {
  visaAlvedon?: boolean;
  visaIpren?: boolean;
  visaCitodon?: boolean;
  extraInfo?: string;
}

const { 
  visaAlvedon = true,
  visaIpren = true,
  visaCitodon = true,
  extraInfo
} = Astro.props;
---

<section class="mb-12">
  <div class="flex items-center gap-4 mb-6">
    <div class="w-12 h-12 rounded-full bg-orange-100 flex items-center justify-center">
      <img src="/images/operation/tablett-24x24.svg" alt="" class="w-8 h-8" />
    </div>
    <h2 class="text-2xl md:text-3xl font-bold text-[#024264]">Smärtstillande</h2>
  </div>

  <div class="bg-white rounded-2xl shadow-lg border border-gray-100 p-6">
    
    <p class="text-slate-600 mb-6">
      De första dygnen kan du behöva smärtstillande. Använd receptfria preparat i första hand.
      Ta smärtstillande <strong>regelbundet de första dagarna</strong> – vänta inte tills det gör ont.
    </p>

    <div class="grid md:grid-cols-3 gap-4 mb-6">
      
      {visaAlvedon && (
        <div class="bg-blue-50 rounded-xl p-4 border border-blue-100">
          <h3 class="font-bold text-blue-800 mb-2">Alvedon® (Paracetamol)</h3>
          <p class="text-sm text-blue-700 mb-2">1g × 4 per dygn</p>
          <p class="text-xs text-blue-600">Grundläggande smärtstillande. Max 4g/dygn.</p>
        </div>
      )}
      
      {visaIpren && (
        <div class="bg-green-50 rounded-xl p-4 border border-green-100">
          <h3 class="font-bold text-green-800 mb-2">Ipren® (Ibuprofen)</h3>
          <p class="text-sm text-green-700 mb-2">400mg × 3 per dygn</p>
          <p class="text-xs text-green-600">Antiinflammatoriskt. Ta med mat. Ej vid magsår.</p>
        </div>
      )}
      
      {visaCitodon && (
        <div class="bg-amber-50 rounded-xl p-4 border border-amber-100">
          <h3 class="font-bold text-amber-800 mb-2">Citodon® (receptbelagt)</h3>
          <p class="text-sm text-amber-700 mb-2">Vid behov, max 8 tabletter/dygn</p>
          <p class="text-xs text-amber-600">Starkare. Ger trötthet. Ej bilkörning!</p>
        </div>
      )}
      
    </div>

    <div class="bg-rose-50 border border-rose-200 rounded-xl p-4">
      <h4 class="font-medium text-rose-800 mb-2 flex items-center gap-2">
        <span>⚠️</span> Viktigt att veta
      </h4>
      <ul class="text-sm text-rose-700 space-y-1">
        <li>• Kombinera <strong>inte</strong> Alvedon med andra paracetamolinnehållande läkemedel</li>
        <li>• Citodon och andra opioidpreparat gör att du <strong>inte får köra bil</strong></li>
        <li>• Kontakta oss om smärtan ökar eller inte lindras efter några dagar</li>
      </ul>
    </div>

  </div>

  {extraInfo && (
    <div class="mt-4 bg-blue-50 border border-blue-200 rounded-xl p-4 text-sm text-blue-800">
      {extraInfo}
    </div>
  )}
</section>

