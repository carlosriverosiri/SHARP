---
/**
 * OpBilkorning - Information om bilkörning efter operation
 */
interface Props {
  timmarEfterNarkos?: number;
  harAxellas?: boolean;
  extraInfo?: string;
}

const { 
  timmarEfterNarkos = 24,
  harAxellas = false,
  extraInfo
} = Astro.props;
---

<section class="mb-8">
  <div class="bg-white rounded-xl shadow-md border border-gray-100 p-4">
    <!-- Rubrik med ikon -->
    <div class="flex items-center gap-3 mb-3 pb-3 border-b border-gray-100">
      <img src="/images/operation/bil-24x24.svg" alt="" class="w-10 h-10" />
      <h2 class="text-lg font-bold text-[#024264]">Köra bil</h2>
    </div>

    <p class="text-slate-700 mb-3">
      Det går bra att köra bil <strong>{timmarEfterNarkos} timmar</strong> efter din operation – 
      då har narkosmedicinerna försvunnit ur kroppen.
    </p>

    <!-- Varning: Smärtstillande -->
    <div class="bg-amber-50 border-l-4 border-amber-400 px-3 py-2 text-sm mb-2">
      <p class="text-amber-800">
        <strong>⚠️ Obs:</strong> Vissa smärtstillande (Citodon®, Tramadol) gör att du <strong>inte får köra bil</strong>.
      </p>
    </div>

    {/* Varning: Axellås */}
    {harAxellas && (
      <div class="bg-red-50 border-l-4 border-red-400 px-3 py-2 text-sm">
        <p class="text-red-800">
          <strong>🚫 Axellås:</strong> Du bör inte köra bil så länge du bär axellåset.
        </p>
      </div>
    )}
  </div>

  {extraInfo && (
    <div class="mt-4 bg-blue-50 border border-blue-200 rounded-xl p-4 text-sm text-blue-800">
      {extraInfo}
    </div>
  )}
</section>

