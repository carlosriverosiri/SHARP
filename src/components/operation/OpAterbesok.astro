---
/**
 * OpAterbesok - Information om återbesök
 */
interface Props {
  typ?: 'standard' | 'sutur' | 'frozen-shoulder';
  efterManader?: number;
  extraInfo?: string;
}

const { 
  typ = 'standard',
  efterManader = 3,
  extraInfo
} = Astro.props;

const typInfo = {
  'standard': {
    title: 'Kontroll vid behov',
    beskrivning: 'De flesta patienter som opereras blir bättre efter operationen. Komplikationer är ovanliga. Därför kontrolleras du bara om du själv, eller din sjukgymnast anser att det finns ett behov av en kontroll.',
    tips: 'Normalt ska man räkna med att det tar 3-4 månader innan man ser resultatet av operationen. Har du efter 3 månader blivit betydligt bättre än du var innan operationen så behöver du ingen kontroll.'
  },
  'sutur': {
    title: 'Planerat återbesök',
    beskrivning: `Om du har blivit opererad med sutur av en avriven sena eller att axeln glider ur led, så kontrolleras du ${efterManader} månader efter operationen.`,
    tips: 'Du får en kallelse med tid för återbesöket.'
  },
  'frozen-shoulder': {
    title: 'Kontroll vid behov',
    beskrivning: 'Om du inte blivit bättre inom 4 veckor efter operationen är det viktigt att du bokar en tid för kontroll.',
    tips: 'Då kan det vara nödvändigt att dämpa en eventuell inflammation i leden med en kortisoninjektion.'
  }
};

const info = typInfo[typ];
---

<section id="aterbesok" class="mb-12 scroll-mt-32">
  <div class="flex items-center gap-4 mb-6">
    <div class="w-12 h-12 rounded-full bg-orange-100 flex items-center justify-center">
      <!-- TODO: IKON SAKNAS - Återbesök/kalender -->
      <span class="text-2xl">📅</span>
    </div>
    <h2 class="text-2xl md:text-3xl font-bold text-[#024264]">Återbesök</h2>
  </div>

  <div class="bg-white rounded-2xl shadow-lg border border-gray-100 p-6">
    
    <h3 class="font-bold text-slate-800 text-lg mb-3">{info.title}</h3>
    
    <p class="text-slate-600 mb-4">
      {info.beskrivning}
    </p>

    <div class="bg-blue-50 rounded-xl p-4 border border-blue-200 mb-6">
      <p class="text-sm text-blue-700">
        💡 {info.tips}
      </p>
    </div>

    <!-- Boka tid -->
    <div class="bg-gray-50 rounded-xl p-4 border border-gray-200">
      <h4 class="font-medium text-slate-800 mb-3">Behöver du boka återbesök?</h4>
      <p class="text-sm text-slate-600 mb-4">
        Enklast är att du själv ringer till mottagningen och bokar en tid.
      </p>
      <div class="flex flex-wrap gap-3">
        <a href="tel:+46855110422" class="inline-flex items-center gap-2 bg-[#024264] text-white px-4 py-2 rounded-full text-sm font-medium hover:bg-blue-800 transition-colors">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
          </svg>
          Ring 08-55 11 04 22
        </a>
        <a href="/fritt-vardval-sverige" class="inline-flex items-center gap-2 bg-white text-[#024264] border-2 border-[#024264] px-4 py-2 rounded-full text-sm font-medium hover:bg-blue-50 transition-colors">
          Boka via hemsidan
        </a>
      </div>
    </div>

  </div>

  {extraInfo && (
    <div class="mt-4 bg-blue-50 border border-blue-200 rounded-xl p-4 text-sm text-blue-800">
      {extraInfo}
    </div>
  )}
</section>

