---
/**
 * OpSjukgymnastik - Information om sjukgymnastik/fysioterapi
 */
interface Props {
  startEfter?: string;      // ex: "1 vecka", "6 veckor"
  antalGanger?: string;     // ex: "1-2 gånger/vecka"
  langd?: string;           // ex: "3-4 månader"
  extraInfo?: string;
}

const { 
  startEfter = "1 vecka",
  antalGanger,
  langd,
  extraInfo
} = Astro.props;
---

<section class="mb-12">
  <div class="flex items-center gap-4 mb-6">
    <div class="w-12 h-12 rounded-full bg-orange-100 flex items-center justify-center">
      <img src="/images/operation/sjukgymnastik-24x24.svg" alt="" class="w-8 h-8" />
    </div>
    <h2 class="text-2xl md:text-3xl font-bold text-[#024264]">Sjukgymnastik</h2>
  </div>

  <div class="bg-white rounded-2xl shadow-lg border border-gray-100 p-6">
    
    <div class="grid md:grid-cols-3 gap-6 mb-6">
      
      <div class="text-center">
        <div class="w-16 h-16 mx-auto mb-3 rounded-full bg-emerald-100 flex items-center justify-center">
          <span class="text-2xl">📅</span>
        </div>
        <p class="text-sm text-slate-500">Börjar efter</p>
        <p class="text-xl font-bold text-emerald-700">{startEfter}</p>
      </div>
      
      {antalGanger && (
        <div class="text-center">
          <div class="w-16 h-16 mx-auto mb-3 rounded-full bg-blue-100 flex items-center justify-center">
            <span class="text-2xl">🔄</span>
          </div>
          <p class="text-sm text-slate-500">Frekvens</p>
          <p class="text-xl font-bold text-blue-700">{antalGanger}</p>
        </div>
      )}
      
      {langd && (
        <div class="text-center">
          <div class="w-16 h-16 mx-auto mb-3 rounded-full bg-purple-100 flex items-center justify-center">
            <span class="text-2xl">🎯</span>
          </div>
          <p class="text-sm text-slate-500">Total tid</p>
          <p class="text-xl font-bold text-purple-700">{langd}</p>
        </div>
      )}
      
    </div>

    <div class="bg-emerald-50 rounded-xl p-4 border border-emerald-200">
      <h4 class="font-medium text-emerald-800 mb-2">📋 Praktisk information</h4>
      <ul class="text-sm text-emerald-700 space-y-1">
        <li>• Du får en remiss till sjukgymnast efter operationen</li>
        <li>• <strong>Boka tid hos en sjukgymnast i god tid innan operationen</strong></li>
        <li>• Följ sjukgymnastens instruktioner noga för bästa resultat</li>
      </ul>
    </div>

  </div>

  {extraInfo && (
    <div class="mt-4 bg-blue-50 border border-blue-200 rounded-xl p-4 text-sm text-blue-800">
      {extraInfo}
    </div>
  )}
</section>

