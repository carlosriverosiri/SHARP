---
/**
 * OpSjukgymnastik - Information om sjukgymnastik/fysioterapi
 */
interface Props {
  startEfter?: string;      // ex: "1 vecka", "6 veckor"
  antalGanger?: string;     // ex: "1-2 gånger/vecka"
  langd?: string;           // ex: "3-4 månader"
  extraInfo?: string;
}

const { 
  startEfter = "1 vecka",
  antalGanger,
  langd,
  extraInfo
} = Astro.props;
---

<section id="sjukgymnastik" class="mb-8 scroll-mt-32">
  <div class="md:bg-white md:rounded-xl md:shadow-md md:border md:border-gray-100 overflow-hidden">
    <!-- Dekorativ banner med fysioaktiviteter -->
    <div class="w-full h-12 overflow-hidden">
      <img 
        src="/images/operation/physio-multiple.svg" 
        alt="" 
        class="w-full h-full object-cover"
      />
    </div>
    
    <div class="p-4">
      <!-- Rubrik -->
      <h2 class="text-lg font-bold text-[#024264] mb-3">Sjukgymnastik</h2>

      <!-- Kompakt info-grid -->
      <div class="flex flex-wrap gap-4 mb-3">
        <div>
          <span class="text-slate-500">Börjar efter:</span>
          <span class="font-semibold text-emerald-700 ml-1">{startEfter}</span>
        </div>
        {antalGanger && (
          <div>
            <span class="text-slate-500">Frekvens:</span>
            <span class="font-semibold text-blue-700 ml-1">{antalGanger}</span>
          </div>
        )}
        {langd && (
          <div>
            <span class="text-slate-500">Total tid:</span>
            <span class="font-semibold text-purple-700 ml-1">{langd}</span>
          </div>
        )}
      </div>

      <!-- Praktisk info (kompakt) -->
      <div class="bg-emerald-50 border-l-4 border-emerald-400 px-3 py-2 text-sm text-emerald-700">
        <strong>📋 Viktigt:</strong> Boka tid hos sjukgymnast i god tid innan operationen. Du får remiss efter op.
      </div>
    </div>
  </div>

  {extraInfo && (
    <div class="mt-4 bg-blue-50 border border-blue-200 rounded-xl p-4 text-sm text-blue-800">
      {extraInfo}
    </div>
  )}
</section>

