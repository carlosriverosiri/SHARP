---
/**
 * OpSjukskrivning - Sjukskrivningsinformation
 */
interface Props {
  heltid: string;           // ex: "1 vecka"
  deltid?: string;          // ex: "1-2 veckor"  
  tungtArbete?: string;     // ex: "6 veckor"
  kommentar?: string;
}

const { 
  heltid, 
  deltid, 
  tungtArbete,
  kommentar
} = Astro.props;
---

<section id="sjukskrivning" class="mb-12 scroll-mt-32">
  <div class="flex items-center gap-4 mb-6">
    <div class="w-12 h-12 rounded-full bg-orange-100 flex items-center justify-center">
      <img src="/images/operation/forsakringskassan-24-24.svg" alt="" class="w-8 h-8" />
    </div>
    <h2 class="text-2xl md:text-3xl font-bold text-[#024264]">Sjukskrivning</h2>
  </div>

  <div class="bg-white rounded-2xl shadow-lg border border-gray-100 overflow-hidden">
    
    <div class="p-6">
      <p class="text-slate-600 mb-6">
        Du får ett sjukintyg efter operationen. Sjukskrivningstiden beror på ditt arbete och operationstyp.
      </p>

      <!-- Tabell -->
      <div class="overflow-x-auto mb-6">
        <table class="w-full">
          <thead>
            <tr class="bg-slate-100">
              <th class="text-left px-4 py-3 font-semibold text-slate-700 rounded-l-lg">Typ av sjukskrivning</th>
              <th class="text-left px-4 py-3 font-semibold text-slate-700 rounded-r-lg">Tid</th>
            </tr>
          </thead>
          <tbody class="divide-y divide-gray-100">
            <tr>
              <td class="px-4 py-3 text-slate-600">
                <div class="flex items-center gap-2">
                  <span class="w-3 h-3 rounded-full bg-red-400"></span>
                  Heltidssjukskrivning
                </div>
              </td>
              <td class="px-4 py-3 font-medium text-slate-800">{heltid}</td>
            </tr>
            {deltid && (
              <tr>
                <td class="px-4 py-3 text-slate-600">
                  <div class="flex items-center gap-2">
                    <span class="w-3 h-3 rounded-full bg-amber-400"></span>
                    Deltidssjukskrivning
                  </div>
                </td>
                <td class="px-4 py-3 font-medium text-slate-800">{deltid}</td>
              </tr>
            )}
            {tungtArbete && (
              <tr>
                <td class="px-4 py-3 text-slate-600">
                  <div class="flex items-center gap-2">
                    <span class="w-3 h-3 rounded-full bg-blue-400"></span>
                    Tungt/fysiskt arbete
                  </div>
                </td>
                <td class="px-4 py-3 font-medium text-slate-800">{tungtArbete}</td>
              </tr>
            )}
          </tbody>
        </table>
      </div>

      <!-- Info om förlängning -->
      <div class="bg-blue-50 rounded-xl p-4 border border-blue-200">
        <h4 class="font-medium text-blue-800 mb-2">📋 Om du behöver förlängning</h4>
        <p class="text-sm text-blue-700">
          Behöver du diskutera en förlängning av sjukskrivningen tar du kontakt med 
          <strong>din husläkare</strong> eller den doktor som remitterade dig till oss.
        </p>
      </div>

      {kommentar && (
        <div class="mt-4 bg-amber-50 border border-amber-200 rounded-xl p-4 text-sm text-amber-800">
          <strong>💡 Observera:</strong> {kommentar}
        </div>
      )}
    </div>

  </div>
</section>

