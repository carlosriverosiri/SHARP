---
/**
 * OpSjukskrivning - Sjukskrivningsinformation
 */
interface Props {
  heltid: string;           // ex: "1 vecka"
  deltid?: string;          // ex: "1-2 veckor"  
  tungtArbete?: string;     // ex: "6 veckor"
  kommentar?: string;
}

const { 
  heltid, 
  deltid, 
  tungtArbete,
  kommentar
} = Astro.props;
---

<section id="sjukskrivning" class="mb-8 scroll-mt-32">
  <div class="bg-white rounded-xl shadow-md border border-gray-100 p-4">
    <!-- Rubrik med ikon -->
    <div class="flex items-center gap-3 mb-3 pb-3 border-b border-gray-100">
      <img src="/images/operation/forsakringskassan-24-24.svg" alt="" class="w-10 h-10" />
      <h2 class="text-lg font-bold text-[#024264]">Sjukskrivning</h2>
    </div>

    <!-- Kompakt tabell -->
    <div class="mb-3">
      <table class="w-full">
        <tbody class="divide-y divide-gray-100">
          <tr>
            <td class="py-2 text-slate-600 flex items-center gap-2">
              <span class="w-2 h-2 rounded-full bg-red-400"></span>
              Heltid
            </td>
            <td class="py-2 font-medium text-slate-800 text-right">{heltid}</td>
          </tr>
          {deltid && (
            <tr>
              <td class="py-2 text-slate-600 flex items-center gap-2">
                <span class="w-2 h-2 rounded-full bg-amber-400"></span>
                Deltid
              </td>
              <td class="py-2 font-medium text-slate-800 text-right">{deltid}</td>
            </tr>
          )}
          {tungtArbete && (
            <tr>
              <td class="py-2 text-slate-600 flex items-center gap-2">
                <span class="w-2 h-2 rounded-full bg-blue-400"></span>
                Tungt arbete
              </td>
              <td class="py-2 font-medium text-slate-800 text-right">{tungtArbete}</td>
            </tr>
          )}
        </tbody>
      </table>
    </div>

    <p class="text-sm text-slate-500">
      📋 Behöver du förlängning? Kontakta din husläkare eller remitterande läkare.
    </p>

    {kommentar && (
      <div class="mt-2 bg-amber-50 border-l-4 border-amber-400 px-3 py-2 text-sm text-amber-800">
        <strong>💡</strong> {kommentar}
      </div>
    )}
  </div>
</section>

